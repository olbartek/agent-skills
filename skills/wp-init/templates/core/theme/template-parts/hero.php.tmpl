<?php
if (!defined('ABSPATH')) { exit; }
?>
<section class="hero">
  <div class="wrap hero__inner">
    <div class="hero__copy">
      <h1 class="hero__title"><?php bloginfo('name'); ?></h1>
      <p class="hero__lead"><?php bloginfo('description'); ?></p>
      <?php if (function_exists('{{PREFIX}}_icon')): ?>
        <a class="button button--primary" href="#main-content">
          <?php esc_html_e('Learn more', '{{THEME_SLUG}}'); ?>
          <?php echo {{PREFIX}}_icon('arrow', 14); ?>
        </a>
      <?php endif; ?>
    </div>
  </div>
</section>
